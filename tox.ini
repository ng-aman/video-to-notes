; [tox]
; envlist = py312
; skipsdist = True
 
; [testenv]
; deps =
;     pytest
;     coverage
; commands =
;     coverage run -m doctest lambda_function.py
;     coverage run -m pytest
;     coverage xml
 
; [coverage:run]
; relative_files = True
; source = .
; branch = True

[tox]
envlist = py312
skipsdist = True

[testenv]
deps =
    coverage
    boto3
    python-dotenv
commands =
    ; coverage run -m unittest discover
    coverage xml

[coverage:run]
relative_files = True
source = .
branch = True
